language: go
go:
  - master

install:
  - go get github.com/magefile/mage
  - go get -d github.com/gohugoio/hugo
  - cd ${GOPATH:-$HOME/go}/src/github.com/gohugoio/hugo
  - mage vendor
  - mage install

before-script:
  - rm -rf public || exit 0

script:
  - cd $TRAVIS_BUILD_DIR
  - hugo

deploy:
  provider: pages
  local_dir: public
  repo_slug: joehickey/joehickey.github.io
  target_branch: public
  github_token: $GITHUB_TOKEN
  skip_cleanup: true
  keep-history: false
  on:
    branch: master
